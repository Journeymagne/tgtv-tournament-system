{
  "name": "kill-team-elo",
  "version": "1.0.0",
  "private": true,
  "description": "Local Kill Team Approved Ops challenge and Elo tracker.",
  "scripts": {
    "build": "node -e \"console.log('Netlify build: public assets are ready')\"",
    "start": "node server.js"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
